#!/usr/bin/awk -f

# reading each line, track whether we are within a PEM block,
# printing each qualifying line

/^-----BEGIN/ { inside_block=1 }
{ if (inside_block) print }
/^-----END/ { inside_block=0 }
