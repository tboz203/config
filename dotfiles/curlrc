# ~/.curlrc - additional command line options to curl(1)

# # write a trailing newline on success
# --write-out "\n"

# request compressed responses, if available
--compressed

# defaut maximum request time of 2 minutes (which should hopefully be plenty for all usecases)
--max-time 120

# --write-out "%{stderr}[cURL] (response code: %{response_code}) %{url}\n"
