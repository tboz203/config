#!/usr/bin/env python

import keyring

kr = keyring.get_keyring()

if isinstance(kr, keyring.backends.fail.Keyring):
    print('Keyring looks broken')
    exit(1)
else:
    print('Keyring looks okay')
    exit(0)
